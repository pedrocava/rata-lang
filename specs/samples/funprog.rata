
is_even = function(n: int) { n %% 2 == 0 }

numbers = 1..10

Enum.map(numbers, is_even)
Enum.map(numbers, ~ .x + 1)

assert Enum.reduce(numbers, ~ .x + .y) == 55

Enum.keep(numbers, is_even) # returns 2, 4, 6, 8, 10
Enum.every(numbers, is_even) # FALSE



defmodule RataParser.Parser do
  @moduledoc """
  Parser for the Rata programming language (temporarily stubbed for CLI compilation).
  """

  # Stub functions to allow compilation
  def parse(_input), do: {:error, "Parser temporarily disabled"}
  def parse_repl(_input), do: {:error, "Parser temporarily disabled"}
end